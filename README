Запуск проекта: sudo docker compose up -d из папки с docker-compose
Маршруты: 
1. http://localhost:8083/send.php
Принимает в качестве параметров url (на который будет отправлять запрос) и file (прикреплённый файл, который будет отправлен).
2. http://nginx-webformat2/recieve.php - Скрипт, получающий данные и сохраняющий их в папку src/files.
Принимает в качестве параметров поля file и filehash. Авторизация на ветке main для данного маршрута происходит через заголовок Api-Key, а на ветке cockie_authorization - через поля login и password.

Страницы (присутствуют только на ветке cockie_authorization):
http://localhost:8083/login.html - логин
http://localhost:8083/send.html - отправка файла

